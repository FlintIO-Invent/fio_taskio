from .beta_registration_link import Command as BetaRegistrationLinkCommand


class Command(BetaRegistrationLinkCommand):
    help = "Print the configured reusable hidden Beta business registration link."
